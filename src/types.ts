export type Chatbot = {
    id: string;
    name: string;
}

export type Chat = {
    id: string;
    phone_number: string;
    name: string;
    last_message_date: string;
    last_message: string;
}

export type UserInfo = {
    id: string;
    name: string;
    email: string;
}